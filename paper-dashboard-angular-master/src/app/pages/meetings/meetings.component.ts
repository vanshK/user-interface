import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'meeting-cmp',
  moduleId: module.id,
  templateUrl: 'meetings.component.html',
})

export class MeetingsComponent implements OnInit{
    ngOnInit(){
    }
}